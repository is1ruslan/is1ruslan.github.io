{
  "name": "liquidityre",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "export": "next build && next export",
    "deploy": "next build && next export && touch out/.nojekyll && git add out && git commit -m 'Deploy Next.js to GitHub Pages' && git push origin `git subtree split --prefix out main`:gh-pages --force"
  },
  "dependencies": {
    "@vercel/analytics": "^1.2.2",
    "axios": "^1.6.7",
    "cheerio": "^1.0.0-rc.12",
    "next": "14.1.2",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "autoprefixer": "^10.0.1",
    "eslint": "^8",
    "eslint-config-next": "14.1.2",
    "postcss": "^8",
    "tailwindcss": "^3.3.0"
  }
}
